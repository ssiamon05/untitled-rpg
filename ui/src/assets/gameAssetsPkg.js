const Actions = require('./Actions/actionsPkg');
const Items = require('./Items/itemsPkg');
const NPC = require('./NPC/npcPkg');
const PC = require('./PC/pcPkg');

const gameAssets = {
    Actions,
    Items,
    NPC,
    PC,
}

module.exports = gameAssets;